{
  "scripts": {
    "dev": "vite",
    "build": "npm run build:css && npm run build:js",
    "build:css": "npx tailwindcss -i resources/css/app.css -o public/build/app.css --minify",
    "build:js": "node -e \"console.log('Skipping Vite build for Railway...')\"",
    "postinstall": "rm -f package-lock.json && npm cache clean --force"
  }
}